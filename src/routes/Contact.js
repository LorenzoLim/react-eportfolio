import React, { Component } from 'react';
import NavBar from '../components/navbar';

class Home extends Component {
  render() {
    return (
      <div>
        <NavBar />
      </div>
    );
  }
}

export default Home;
